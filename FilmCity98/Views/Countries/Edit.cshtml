@model FilmCity98.Models.Country

@{
    ViewData["Title"] = "Edit Country";
}

<h1 class="text-center my-4">Edit Country</h1>

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-lg p-4">
                <h4 class="card-title text-center mb-4">Country Details</h4>
                <hr />
                <form asp-action="Edit">
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>
                    <input type="hidden" asp-for="Id" />

                    <div class="form-group">
                        <label asp-for="Name" class="control-label font-weight-bold"></label>
                        <input asp-for="Name" type="date" class="form-control form-control-lg" placeholder="Enter country name" />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>

                    <div class="form-group text-center">
                        <button type="submit" value="Save" class="btn btn-primary btn-lg px-5">Save</button>
                        <a asp-action="Index" class="btn btn-secondary btn-lg ml-3">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
